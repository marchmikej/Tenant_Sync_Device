<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:keepScreenOn="true"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MaintenanceHome">
    <ImageButton
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:textSize="25sp"
        android:layout_marginTop="30dp"
        android:layout_alignParentTop="true"
        android:id="@+id/goback"
        android:src="@drawable/go_back_button"
        android:onClick="goback" />

    <Button
        android:layout_width="670dp"
        android:layout_height="70dp"
        android:textSize="25sp"
        android:text="Create Maintenance"
        android:drawableLeft="@drawable/maintenance_icon_64"
        android:layout_below="@+id/goback"
        android:id="@+id/createMaintenance"
        android:onClick="createMaintenance" />


    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/createMaintenance" >

    </ListView>

</RelativeLayout>